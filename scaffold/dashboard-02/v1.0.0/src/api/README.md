# API

This directory contains auto-generated TypeScript RPC clients.

**Do NOT edit files in this directory manually.**

To regenerate, modify the Thrift IDL in `api/` and run:

```bash
gve api generate
```
